import { styled } from 'styled-components'

export const HomeContainer = styled.div`
  display: flex;
  flex-direction: column;
  gap: 2rem;

  .container {
    margin-left: auto;
    margin-right: auto;
    width: 1140px;
  }
`
